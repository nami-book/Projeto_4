import styled from "styled-components"
import { colors } from "styles/GlobalStyles"

export const Section = styled.section`
  background-color: ${colors.whitesmoker};
  box-shadow: 0px 4px 4px ${colors.bg};
  margin: 0 5px;
  padding: 1.2rem;
`